<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partials/head'); -%>
    </head>

    <body>
        <div class="pageBody">
            <%- include('../partials/nav_bar'); %>

            <h3>Users home page</h3>
            <p><%= user %></p>

            <div class="userLogo">
                <img src="imgs/cc_logo.png"/>
            </div>

            <div class="portalBottom">
                <div class="short">
                <img src="imgs/short_landscape.png"/>
                </div>
                <div class="long">
                <img src="imgs/long_landscape.png"/>
                </div>
            </div>

            <footer>
                <%- include('../partials/footer'); %>
            </footer>
        </div>
        <script>
            document.getElementById("navLinks").addEventListener("load", displayLinks);

            function displayLinks() {
                console.log('here');

                const rr = document.getElementById('rr');
            
                console.log(user.role);

                if (user.role === 'rr' || user.role == 'king') {
                    rr.style.display = 'block';
                }
            }
        </script>
    </body>
</html>